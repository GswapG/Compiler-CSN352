// #include <stdio.h>

// Define an enum for colors
enum Color {
    RED,
    GREEN,
    BLUE
};

int main() {
    // Declare an enum variable and assign a value
    enum Color favoriteColor = GREEN;

    int abc , abc3 = abc2 ;
    
    // Print the enum value (typically represented as an integer)
    printf("Favorite color code: %d\n", favoriteColor);
    return 0;
}
